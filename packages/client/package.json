{
  "name": "@atlas/client",
  "version": "0.0.0",
  "private": true,
  "description": "Browser client for Atlas (Phaser 3 + Vite). Renders server events; sends intents only.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@atlas/engine": "*",
    "@atlas/shared": "*",
    "colyseus.js": "^0.16.0",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
